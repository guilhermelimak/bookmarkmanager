import CommonButton from './CommonButton'
import { shallow } from 'vue-test-utils'

describe('CommonButton test', () => {
  it('should work?', () => {
    const wrapper = shallow(CommonButton);
  });
});
