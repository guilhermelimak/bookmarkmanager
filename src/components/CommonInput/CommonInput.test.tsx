import CommonInput from './CommonInput'
import { shallow } from 'vue-test-utils'

describe('CommonInput test', () => {
  it('should work?', () => {
    const wrapper = shallow(CommonInput);
  });
});
