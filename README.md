## Bookmarkmanager

## Build Setup

```bash
Add a .env file on the projec root with the firebase key
API_KEY=<your-firebase-key-here>

# install dependencies
yarn

# serve with hot reload at localhost:8080
yarn dev

# build for production with minification
yarn build

# run unit tests
yarn test

# generate a new component
yarn generate
```
